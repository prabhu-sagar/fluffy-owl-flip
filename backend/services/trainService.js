export const getTrainRoutes = async (source, destination, date) => {
  // Mock implementation - replace with real API call
  return [
    { id: 't1', mode: 'train', from: source, to: destination, cost: 1200, duration: 480 }
  ];
};