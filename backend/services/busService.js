export const getBusRoutes = async (source, destination, date) => {
  // Mock implementation - replace with real API call
  return [
    { id: 'b1', mode: 'bus', from: source, to: destination, cost: 800, duration: 600 }
  ];
};